# Psychology Content Orchestrator Agent Requirements

# Core Pydantic AI framework
pydantic-ai>=0.0.15
pydantic>=2.9.0
pydantic-settings>=2.6.0

# Async operations and coordination
asyncio>=3.8.0
httpx>=0.24.0

# Environment configuration
python-dotenv>=1.0.0

# Data processing and orchestration
pandas>=2.0.0
numpy>=1.24.0

# Task scheduling and coordination
APScheduler>=3.10.0  # For scheduling orchestration tasks
asyncio-throttle>=1.0.0  # For rate limiting agent calls

# Optional: Inter-process communication
celery>=5.3.0  # For distributed task queue (if needed)
redis>=4.5.0   # For task coordination cache

# Optional: Project management integration
python-archon>=1.0.0  # For Archon integration (if available)

# Development dependencies (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.10.0