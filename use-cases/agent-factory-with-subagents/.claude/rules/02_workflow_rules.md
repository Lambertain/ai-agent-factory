# 02. Правила робочого процесу (Workflow)

## 🎯 Чотири критичних правила workflow

### 1️⃣ ПРАВИЛО ЗАНУРЕННЯ В РОЛЬ

**Обов'язкове занурення в контекст експерта:**

- 📖 **ЧИТАЙ системний промпт** ролі перед роботою
- 🧠 **ЗАГЛИБЛЮЙСЯ** у спеціалізацію та експертизу
- 🎭 **ПРАЦЮЙ** як експерт у цій області, а не як загальний AI
- ✅ **ВИКОРИСТОВУЙ** специфічну термінологію та підходи ролі

**Приклад неправильного підходу:**
```
❌ "Створюю компонент React..."
   (працює як загальний програміст)
```

**Приклад правильного підходу:**
```
✅ "Як UI/UX Enhancement Agent, проектую компонент з фокусом на:
   - Accessibility (WCAG 2.1 AA)
   - Responsive дизайн
   - Користувацький досвід
   - Взаємодія з shadcn/ui бібліотекою"
```

### 2️⃣ ПРАВИЛО ГНУЧКИХ СТАТУСІВ

**Вибирай правильний статус задачі по підсумку виконання:**

```python
# СЦЕНАРІЙ 1: Повністю виконана ✅
if task_completed_successfully and no_issues_found:
    status = "done"
    # Приклад: реалізував фічу, всі тести проходять, код-рев'ю чистий

# СЦЕНАРІЙ 2: Вимагає перевірки 🔍
elif task_completed_but_needs_review:
    status = "review"
    # Приклад: код готовий, але потрібна валідація від іншого агента
    # Важливо: review задачі можуть блокувати інші задачі!

# СЦЕНАРІЙ 3: Потрібна ескалація ⚠️
elif needs_escalation_or_help:
    status = "doing"  # Залишити в статусі "doing"
    # Створити нову задачу для агента, який може допомогти
    await mcp__archon__manage_task(
        action="create",
        title="[Ескалація] Допомога з [проблема]",
        description="Потрібна допомога: [деталі]",
        assignee="[відповідний агент]"
    )

# СЦЕНАРІЙ 4: Заблокована 🚫
elif task_blocked:
    status = "doing"
    # Оновити опис із інформацією про блокер
    description += "\n\n🚫 БЛОКЕР: [опис]\nОчікує: [що потрібно для розблокування]"
```

**КЛЮЧОВА ЛОГІКА:**
- `done` - повністю виконано без питань
- `review` - готово, але вимагає швидкої валідації
- `doing` + ескалація - потрібна допомога, створена задача для іншого агента
- `doing` + блокер - заблоковано зовнішніми факторами

---

## 🔴 ЧЕРВОНІ ЛІНІЇ: Оновлення статусів задач (ДОДАНО 2025-10-18)

**🚨 КРИТИЧНО: Система з 3 рівнів захисту проти пропущених оновлень статусів**

### ПРОБЛЕМА (ДВА ПРОПУЩЕНИХ КРОКИ):

Агенти забувають оновлювати статус задачі В ДВА МОМЕНТИ:
1. ❌ Перед початком роботи: не змінюють "todo" → "doing"
2. ❌ Після завершення: не змінюють "doing" → "done"

### ПРИКЛАД ПРОБЛЕМИ:
```
Задача створена в статусі "todo" (19:01:45)
❌ Агент взяв в роботу БЕЗ зміни статусу на "doing"
✅ Виконав задачу (22:15:32)
✅ Закомітив зміни (ef6b402)
❌ НЕ оновив статус на "done"
РЕЗУЛЬТАТ: Задача всі ще в "todo" хоча завершена!
```

### РІШЕННЯ: 3 рівні захисту

#### 🛡️ РІВЕНЬ 1: Обов'язкові пункти TodoWrite

**КОЖНА ЗАДАЧА ПОВИННА МАТИ:**

```python
TodoWrite([
    # 🚨 ПЕРШИЙ ПУНКТ - ОБОВ'ЯЗКОВИЙ (НІКОЛИ НЕ ПРОПУСКАТИ!)
    {
        "content": "🚨 ОБОВ'ЯЗКОВО: Оновити статус задачі на 'doing' в Archon",
        "status": "pending",
        "activeForm": "Оновлюю статус задачі на 'doing' в Archon"
    },

    # ... основні пункти роботи (3-15 штук) ...

    # ✅ ОСТАННІЙ ПУНКТ - ОБОВ'ЯЗКОВИЙ (НІКОЛИ НЕ ПРОПУСКАТИ!)
    {
        "content": "✅ ОБОВ'ЯЗКОВО: Post-Task Checklist - оновити статус на 'done', звіт користувачу",
        "status": "pending",
        "activeForm": "Виконую Post-Task Checklist та оновлюю статус на 'done'"
    }
])
```

**ПРАВИЛА:**
- ПЕРШИЙ пункт = обов'язкове оновлення на "doing"
- ОСТАННІЙ пункт = обов'язковий Post-Task Checklist з оновленням на "done"
- ЗАБОРОНЕНО змінювати формулювання цих пунктів
- ЗАБОРОНЕНО пропускати ці пункти

#### 🛡️ РІВЕНЬ 2: ЧЕРВОНІ ЛІНІЇ (абсолютні правила)

##### 🚨 ЧЕРВОНА ЛІНІЯ #1: Робота без статусу "doing"

**ЗАБОРОНЕНО:**
- Брати задачу в роботу без оновлення статусу з "todo" → "doing"
- Створювати TodoWrite без ПЕРШОГО обов'язкового пункту
- Пропускати оновлення статусу "для швидкості"

**ОБОВ'ЯЗКОВА ПОСЛІДОВНІСТЬ:**
```
1. Отримати задачу з Archon MCP
   ↓
2. НЕГАЙНО оновити статус на "doing"
   await mcp__archon__manage_task(
       action="update",
       task_id=task_id,
       status="doing"
   )
   ↓
3. ТІЛЬКИ ПІСЛЯ - створити TodoWrite
   ↓
4. Почати виконання
```

**ПЕРЕВІРКА:**
Якщо ти працюєш над задачею і її статус всі ще "todo" → ЗУПИНИСЬ та оновиш статус!

**МЕХАНІЗМ ЗУПИНКИ:**
- Перед будь-якою дією перевіряй статус задачі
- Якщо статус "todo" а ти вже почав роботу → ЗУПИНКА
- Оновіш статус на "doing" → Продовжуй роботу

##### 🚨 ЧЕРВОНА ЛІНІЯ #2: Git коміт без статусу "done"

**ЗАБОРОНЕНО:**
- Робити git commit завершеної задачі без оновлення статусу
- Закривати задачу без зміни статусу на "done" або "review"
- "Забувати" оновити статус після коміту

**ОБОВ'ЯЗКОВА ПОСЛІДОВНІСТЬ ПЕРЕД КОМІТОМ:**
```
1. Виконати всі пункти задачі
   ↓
2. ОБОВ'ЯЗКОВО оновити статус
   - Якщо виконано без проблем → status="done"
   - Якщо потрібна перевірка → status="review"
   - Якщо блокер → status="doing" + створити задачу допомоги
   ↓
3. ТІЛЬКИ ПІСЛЯ - робити git commit
   ↓
4. Push в репозиторій
```

**ПЕРЕВІРКА:**
Перед кожним комітом:
- Перевір статус задачі в Archon
- Якщо статус "doing" а робота завершена → ЗУПИНИСЬ
- Оновіш статус на "done" або "review"
- ТІЛЬКИ ПОТІМ робіш коміт

**МЕХАНІЗМ ЗУПИНКИ:**
- Перед git commit → перевірити статус задачі
- Якщо статус не "done" та не "review" → ЗУПИНКА
- Оновити статус → Продовжити коміт

##### 🚨 ЧЕРВОНА ЛІНІЯ #3: TodoWrite без обов'язкових пунктів

**ЗАБОРОНЕНО:**
- Створювати TodoWrite без ПЕРШОГО пункту (оновлення на "doing")
- Створювати TodoWrite без ОСТАННЬОГО пункту (Post-Task Checklist з "done")
- Змінювати формулювання обов'язкових пунктів

**ПЕРЕВІРКА:**
Після створення TodoWrite:
- Перевіряй що ПЕРШИЙ пункт = оновлення на "doing"
- Перевіряй що ОСТАННІЙ пункт = Post-Task Checklist з "done"
- Якщо немає → ЗУПИНИСЬ та додай

**МЕХАНІЗМ ЗУПИНКИ:**
- Після TodoWrite → перевірити структуру
- Якщо немає обов'язкових пунктів → ЗУПИНКА
- Пересоздати TodoWrite з обов'язковими пунктами
- ТІЛЬКИ ПОТІМ починати роботу

#### 🛡️ РІВЕНЬ 3: Автоматична валідація (опціонально)

**Файл:** `common/validate_task_status_before_commit.py`

Pre-commit хук який:
- Перевіряє наявність Task ID в сообщенні коміту
- Якщо Task ID знайдено - перевіряє статус задачі через Archon MCP
- Якщо статус "todo" або "doing" - блокує коміт з інструкціями
- Якщо статус "done" або "review" - дозволяє коміт

**Graceful Degradation:** Якщо Archon MCP недоступний - пропускає перевірку з warning.

**Детальна інструкція:** `docs/SETUP_TASK_STATUS_VALIDATION.md`

### 📊 Таблиця ЧЕРВОНИХ ЛІНІЙ

| # | Правило | Перевірка | Дія при порушенні |
|---|---------|-----------|-------------------|
| 1 | Статус "doing" перед роботою | Статус задачі в Archon | СТОП → Оновити статус → Продовжити |
| 2 | Статус "done"/"review" перед комітом | Статус перед git commit | СТОП → Оновити статус → Коміт |
| 3 | TodoWrite з обов'язковими пунктами | Перший та останній пункти | СТОП → Пересоздати TodoWrite → Робота |

**ПОВНА ДОКУМЕНТАЦІЯ:**
- Рівень 1: `.claude/rules/03_task_management.md` - Обов'язкові пункти TodoWrite
- Рівень 2: Цей розділ - ЧЕРВОНІ ЛІНІЇ
- Рівень 3: `docs/SETUP_TASK_STATUS_VALIDATION.md` - Автоматична валідація

---

### 3️⃣ ПРАВИЛО ПРІОРИТИЗАЦІЇ ЧЕРЕЗ ПРОДЖЕКТ-МЕНЕДЖЕРА

**🚨 КРИТИЧНО ВАЖЛИВО: Після завершення задачі ЗАВЖДИ переключайся на роль проджект-менеджера!**

**ПРОБЛЕМА старого підходу:**
- Агент шукає задачі тільки для своєї ролі
- Пропускає задачі з вищим пріоритетом для інших ролей
- Неоптимальне використання ресурсів

**РІШЕННЯ: Централізована оркестрація через проджект-менеджера**

### Правильний workflow після завершення задачі:

```
┌─────────────────────────────────────────────────┐
│ 1. ЗАВЕРШЕННЯ ЗАДАЧІ В ПОТОЧНІЙ РОЛІ           │
│    ✅ Виконання → Рефлексія → Git коміт        │
│    ✅ Оновлення статусу в Archon на "done"     │
└─────────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────────┐
│ 2. ПЕРЕКЛЮЧЕННЯ НА РОЛЬ ПРОДЖЕКТ-МЕНЕДЖЕРА     │
│    🎭 Знайти промпт archon_project_manager     │
│    🎭 Переключитися в роль координатора        │
└─────────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────────┐
│ 3. АНАЛІЗ СТАНУ ПРОЕКТУ (ПРОДЖЕКТ-МЕНЕДЖЕР)    │
│    📊 Перевірити задачі в статусі "doing"      │
│    📊 Перевірити задачі в статусі "review"     │
│    📊 Якщо є doing/review - обрати найвищий    │
│         пріоритет серед них                     │
└─────────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────────┐
│ 4. ВИБІР ЗАДАЧІ З НАЙВИЩИМ ПРІОРИТЕТОМ         │
│    🎯 Якщо є doing/review - обрати з них       │
│    🎯 Якщо НІ doing/review - обрати з todo     │
│         (найвищий task_order серед УСІХ ролей) │
└─────────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────────┐
│ 5. ПЕРЕКЛЮЧЕННЯ В РОЛЬ ДЛЯ ЗАДАЧІ              │
│    🎭 Визначити assignee задачі                │
│    🎭 Переключитися в відповідну роль          │
│    ✅ Почати виконання                         │
└─────────────────────────────────────────────────┘
```

### Алгоритм проджект-менеджера:

```python
async def select_next_highest_priority_task(project_id: str) -> dict:
    """
    Вибрати задачу з найвищим пріоритетом серед УСІХ ролей.
    Викликається ТІЛЬКИ в ролі проджект-менеджера.

    Args:
        project_id: ID проекту (ОБОВ'ЯЗКОВИЙ параметр!)

    🚨 КРИТИЧНО: ЗАВЖДИ передавати project_id для фільтрації задач!
    """

    # КРОК 1: Перевірка doing/review задач (будь-яка роль) у ПОТОЧНОМУ ПРОЕКТІ
    doing_tasks = await mcp__archon__find_tasks(
        project_id=project_id,  # 🚨 ОБОВ'ЯЗКОВО!
        filter_by="status",
        filter_value="doing"
    )
    review_tasks = await mcp__archon__find_tasks(
        project_id=project_id,  # 🚨 ОБОВ'ЯЗКОВО!
        filter_by="status",
        filter_value="review"
    )

    # КРОК 2: Якщо є doing/review - обрати найвищий пріоритет
    urgent_tasks = doing_tasks + review_tasks
    if urgent_tasks:
        task = max(urgent_tasks, key=lambda t: t["task_order"])
        print(f"🔴 ПРІОРИТЕТ: {task['status'].upper()} задача")
        print(f"📌 PROJECT: {project_id}")
        return task

    # КРОК 3: Якщо НІ doing/review - обрати todo з найвищим task_order
    todo_tasks = await mcp__archon__find_tasks(
        project_id=project_id,  # 🚨 ОБОВ'ЯЗКОВО!
        filter_by="status",
        filter_value="todo"
    )
    if todo_tasks:
        task = max(todo_tasks, key=lambda t: t["task_order"])
        print(f"🟢 НОВА ЗАДАЧА з task_order: {task['task_order']}")
        print(f"📌 PROJECT: {project_id}")
        return task

    print("📭 Немає задач для виконання")
    print(f"📌 PROJECT: {project_id}")
    return None
```

### Критичні правила:

**✅ ЗАВЖДИ:**
- Після завершення задачі → переключення на проджект-менеджера
- Проджект-менеджер аналізує ВСІ задачі (не тільки для однієї ролі)
- Вибір задачі з найвищим пріоритетом незалежно від ролі
- Переключення в потрібну роль для виконання обраної задачі

**❌ НІКОЛИ:**
- Не шукати задачі тільки для поточної ролі
- Не пропускати doing/review задачі інших ролей
- Не брати todo задачі якщо є doing/review

**ОБҐРУНТУВАННЯ ПРІОРИТЕТІВ:**
1. **doing/review (будь-яка роль)** - найвищий пріоритет, може блокувати проект
2. **todo (найвищий task_order)** - нова робота, оптимальний вибір серед усіх ролей

---

### 4️⃣ ПРАВИЛО ПЕРЕВІРКИ ІСНУЮЧОГО КОДУ ПЕРЕД ЗМІНАМИ

**🚨 КРАСНА ЛІНІЯ: НІ ОДНОГО РЯДКА КОДУ БЕЗ ПЕРЕВІРКИ ІСНУЮЧОГО!**

**Проблема:** Агенти створюють дублюючий функціонал замість використання існуючого.

**Рішення:** Обов'язковий Grep/Glob пошук ПЕРЕД будь-якими змінами коду.

**ОБОВ'ЯЗКОВА ПОСЛІДОВНІСТЬ ПЕРЕД створенням/зміною/розширенням БУДЬ-ЯКОГО коду:**

```
ЕТАП 1: GREP ПОШУК (ОБОВ'ЯЗКОВО)
├─ Grep пошук за ключовими словами функціоналу
├─ Grep пошук за назвами функцій/класів/компонентів
└─ Grep пошук за схожою бізнес-логікою

ЕТАП 2: GLOB ПОШУК (ОБОВ'ЯЗКОВО)
├─ Glob пошук за патерном імен файлів
├─ Glob пошук за структурою папок
└─ Glob пошук за технологіями (API, DB, UI)

ЕТАП 3: АНАЛІЗ ЗНАЙДЕНОГО (ОБОВ'ЯЗКОВО)
├─ Прочитати ВСІ знайдені файли (Read tool)
├─ Оцінити рівень існуючої імплементації
└─ Показати користувачу ПОВНИЙ СПИСОК знайденого

ЕТАП 4: РІШЕННЯ НА ОСНОВІ АНАЛІЗУ
├─ СЦЕНАРІЙ 1: Існує повна імплементація → ВИКОРИСТАТИ (НЕ створювати)
├─ СЦЕНАРІЙ 2: Існує частковий фундамент → РОЗШИРИТИ (НЕ створювати з нуля)
└─ СЦЕНАРІЙ 3: Нічого не знайдено → СТВОРИТИ новий функціонал
```

**📋 ОБОВ'ЯЗКОВИЙ ЧЕК-ЛИСТ ПЕРЕД ЗМІНАМИ:**

```
☐ 1. Виконав Grep пошук за ключовими словами
☐ 2. Виконав Glob пошук за патерном файлів
☐ 3. Прочитав ВСІ знайдені файли (Read tool)
☐ 4. Проаналізував знайдені результати
☐ 5. Показав користувачу ПОВНИЙ СПИСОК знайденого
☐ 6. Обґрунтував рішення (створити/розширити/змінити/використати)
☐ 7. Отримав підтвердження користувача перед початком
```

**ПОВНИЙ ПРОТОКОЛ:** `.claude/rules/09d_anti_duplication_protocol.md`

**КРИТИЧНІ ПРАВИЛА:**

**✅ ЗАВЖДИ:**
- Перевіряти існуючий функціонал ПЕРЕД створенням нового
- Показувати користувачу результати пошуку
- Обґрунтовувати чому створюємо новий/розширюємо існуючий
- Використовувати існуючий функціонал якщо він є

**❌ НІКОЛИ:**
- Створювати функціонал без Grep/Glob пошуку
- Дублювати код що вже існує
- Змінювати код без перевірки існуючих рішень
- Ігнорувати знайдені файли з схожим функціоналом

**🚨 МЕХАНІЗМ ЗУПИНКИ ПРИ ПОРУШЕННІ:**
```
ЯКЩО агент починає створювати код БЕЗ Grep/Glob:
├─ ЗУПИНИТИСЯ негайно
├─ Виконати обов'язковий пошук
├─ Проаналізувати результати
└─ ТІЛЬКИ після цього продовжувати

ЯКЩО знайдено існуючий функціонал:
├─ ЗАБОРОНЕНО створювати дублікат
├─ ОБОВ'ЯЗКОВО використати або розширити існуюче
└─ Показати користувачу план інтеграції
```

**ПРИКЛАД ПРАВИЛЬНОГО WORKFLOW:**

```
Задача: Додати QR-сканування в додаток

✅ ШАГ 1: Grep пошук
Grep(pattern="qr|scan|camera|barcode", output_mode="files_with_matches")
→ Знайдено: services/scanner.service.ts, utils/qr-parser.ts

✅ ШАГ 2: Glob пошук
Glob(pattern="**/*scan*.ts")
→ Знайдено: components/ScannerModal.tsx, hooks/useQrScanner.ts

✅ ШАГ 3: Читання знайдених файлів
Read(scanner.service.ts) → Є базовий сервіс сканування
Read(qr-parser.ts) → Є парсер QR кодів
Read(ScannerModal.tsx) → Є UI компонент модального вікна
Read(useQrScanner.ts) → Є хук для доступу до камери

✅ ШАГ 4: Аналіз та рішення
Висновок: Існує ПОВНА імплементація QR-сканування
Рішення: ВИКОРИСТАТИ існуючий функціонал

✅ ШАГ 5: Показати користувачу
"📋 Знайдено існуючу імплементацію QR-сканування:
- services/scanner.service.ts - базовий сервіс
- utils/qr-parser.ts - парсер кодів
- components/ScannerModal.tsx - UI компонент
- hooks/useQrScanner.ts - доступ до камери

💡 Рекомендація: Використати існуючі компоненти замість створення нових"
```

---

## 🎯 ПОГРУЖЕННЯ В КОНТЕКСТ ПРОЕКТУ

**ОБОВ'ЯЗКОВА ПОСЛІДОВНІСТЬ ПЕРЕД ПОЧАТКОМ БУДЬ-ЯКОЇ ЗАДАЧІ:**

```
ЕТАП 0: ОТРИМАННЯ ЗАДАЧІ
└─ Отримати task_id → Вилучити project_id

ЕТАП 1: ЧИТАННЯ ОПИСУ ПРОЕКТУ (ОБОВ'ЯЗКОВО)
├─ mcp__archon__find_projects(project_id=project_id)
├─ Запам'ятати: title, description, github_repo, технології
└─ Вивести користувачу: "📋 Проект: [назва]\n📖 Опис: [200 символів]...\n🔗 Репозиторій: [github_repo]"

ЕТАП 2: ЧИТАННЯ ОСТАННІХ КОМІТІВ (ОБОВ'ЯЗКОВО)
├─ Вилучити локальний шлях із github_repo
├─ cd "[local_path]" && git log --oneline -10
└─ Вивести користувачу зведення комітів

ЕТАП 3: ЧИТАННЯ ПРАВИЛ ПРОЕКТУ (ОБОВ'ЯЗКОВО)
└─ Прочитати: [local_path]/.claude/rules.md та CLAUDE.md

ЕТАП 4: ТІЛЬКИ ПІСЛЯ ЦЬОГО
└─ Переключитися в роль → Створити мікрозадачі → Почати виконання
```

---

## ✅ CHECKPOINT ВАЛІДАЦІЯ ПЕРЕД ВИСНОВКАМИ ТА РІШЕННЯМИ

**ПРОБЛЕМА:** Агенти роблять висновки та пропонують рішення БЕЗ читання коду, що призводить до неправильних рішень.

**РІШЕННЯ:** Обов'язкова checkpoint валідація перед КОЖНИМ висновком або рішенням.

### ОБОВ'ЯЗКОВІ CHECKPOINTS:

#### CHECKPOINT 1: Перед аналізом проблеми

**Питання до себе:** "Чи прочитав я ВСІ релевантні файли коду?"

```python
def checkpoint_before_problem_analysis() -> bool:
    """
    Checkpoint перед аналізом проблеми.

    Returns:
        True якщо можна продовжувати, False якщо потрібно читати код
    """

    checklist = {
        "files_identified": has_identified_relevant_files(),  # Визначив які файли читати
        "files_read": len(files_read) >= 3,  # Прочитав мінімум 3 файли
        "shown_to_user": has_shown_file_list_to_user(),  # Показав список файлів користувачу
    }

    if not all(checklist.values()):
        print("⚠️ CHECKPOINT FAIL: Прочитай код перед аналізом!")
        for check, passed in checklist.items():
            status = "✅" if passed else "❌"
            print(f"{status} {check}")
        return False

    return True
```

**Приклад правильного підходу:**
```
User: "Помилка: Rate Limit exceeded на /api/auth/error"

Agent:
🔍 ШАГ 1: Визначаю релевантні файли
- auth.service.ts (обробка авторизації)
- error.middleware.ts (обробка помилок)
- rate-limit.config.ts (конфігурація Rate Limit)

📖 ШАГ 2: Читаю файли
[Read auth.service.ts]
[Read error.middleware.ts]
[Read rate-limit.config.ts]

✅ CHECKPOINT PASSED: Прочитав 3 релевантні файли

🔍 ШАГ 3: Аналізую код...
```

#### CHECKPOINT 2: Перед висновком про причину

**Питання до себе:** "Чи базується мій висновок на ФАКТАХ з коду?"

```python
def checkpoint_before_root_cause_conclusion() -> bool:
    """
    Checkpoint перед висновком про root cause.

    Returns:
        True якщо висновок на основі фактів, False якщо вгадування
    """

    checklist = {
        "code_quotes": has_code_quotes_in_analysis(),  # Є цитати коду
        "file_line_refs": has_file_line_references(),  # Є посилання file:line
        "no_guessing": not uses_guessing_phrases(),  # Немає фраз-вгадувань
        "explains_how": explains_how_code_causes_error(),  # Пояснює ЯК код призводить до помилки
    }

    if not all(checklist.values()):
        print("⚠️ CHECKPOINT FAIL: Висновок не базується на фактах!")
        for check, passed in checklist.items():
            status = "✅" if passed else "❌"
            print(f"{status} {check}")
        print("\n🚨 ЗУПИНИСЬ! Процитуй конкретні рядки коду!")
        return False

    return True
```

**Приклад правильного висновку:**
```
✅ ROOT CAUSE визначена на основі аналізу коду:

В файлі auth.service.ts:42 бачу код:
```typescript
export async function signIn(credentials) {
  await rateLimiter.check();  // ← Викликається КОЖЕН раз
  return await authProvider.signIn(credentials);
}
```

Проблема: rateLimiter.check() викликається ПЕРЕД перевіркою чи користувач вже авторизований.

Коли користувач натискає "Войти", код ЗАВЖДИ викликає Rate Limit checker,
навіть якщо сесія вже активна.

Рішення: Додати перевірку активної сесії ПЕРЕД Rate Limit:
```typescript
export async function signIn(credentials) {
  // НОВИЙ КОД: перевірити активну сесію
  if (await hasActiveSession()) {
    return { already_signed_in: true };
  }
  await rateLimiter.check();
  return await authProvider.signIn(credentials);
}
```
```

#### CHECKPOINT 3: Перед пропозицією рішення

**Питання до себе:** "Чи базується моє рішення на РЕАЛЬНОМУ коді?"

```python
def checkpoint_before_solution_proposal() -> bool:
    """
    Checkpoint перед пропозицією рішення.

    Returns:
        True якщо рішення на основі реального коду, False якщо припущення
    """

    checklist = {
        "specific_file": specifies_exact_file_and_line(),  # Вказує точний файл:рядок
        "shows_fix": shows_how_to_fix_code(),  # Показує ЯК виправити
        "explains_why": explains_why_fix_solves_problem(),  # Пояснює ЧОМУ це вирішує проблему
        "based_on_code": based_on_actual_code_not_assumptions(),  # Базується на реальному коді
    }

    if not all(checklist.values()):
        print("⚠️ CHECKPOINT FAIL: Рішення не базується на реальному коді!")
        for check, passed in checklist.items():
            status = "✅" if passed else "❌"
            print(f"{status} {check}")
        print("\n🚨 ЗУПИНИСЬ! Покажи конкретне виправлення коду!")
        return False

    return True
```

**Приклад правильного рішення:**
```
✅ РІШЕННЯ на основі аналізу коду:

ФАЙЛ: auth.service.ts
РЯДОК: 42
ПРОБЛЕМА: Rate Limit викликається перед перевіркою сесії

ВИПРАВЛЕННЯ:
```typescript
// БУЛО (НЕПРАВИЛЬНО):
export async function signIn(credentials) {
  await rateLimiter.check();  // ❌ Завжди викликається
  return await authProvider.signIn(credentials);
}

// СТАНЕ (ПРАВИЛЬНО):
export async function signIn(credentials) {
  // ✅ Спочатку перевірити активну сесію
  if (await hasActiveSession()) {
    return { already_signed_in: true };
  }
  await rateLimiter.check();
  return await authProvider.signIn(credentials);
}
```

ЧОМУ ЦЕ ВИРІШУЄ ПРОБЛЕМУ:
- Rate Limit НЕ буде викликатися для вже авторизованих користувачів
- Кнопка "Войти" для авторизованого користувача повертає { already_signed_in: true }
- Rate Limit застосовується ТІЛЬКИ для реальних спроб логіну
```

---

### АВТОМАТИЧНА ВАЛІДАЦІЯ В TODOWRITE

**ОБОВ'ЯЗКОВА ОСТАННЯ МІКРОЗАДАЧА** перед пропозицією рішення:

```python
# Приклад TodoWrite для задачі з аналізом проблеми:
await TodoWrite([
    {"content": "Визначити релевантні файли для аналізу", "status": "pending"},
    {"content": "Прочитати ВСІ релевантні файли (мінімум 3)", "status": "pending"},
    {"content": "✅ CHECKPOINT 1: Показати список прочитаних файлів користувачу", "status": "pending"},
    {"content": "Проаналізувати код та процитувати конкретні рядки", "status": "pending"},
    {"content": "✅ CHECKPOINT 2: Перевірити чи висновок базується на фактах", "status": "pending"},
    {"content": "Запропонувати рішення з конкретним виправленням коду", "status": "pending"},
    {"content": "✅ CHECKPOINT 3: Перевірити чи рішення базується на реальному коді", "status": "pending"},
])
```

---

### КРИТИЧНІ ПРАВИЛА:

**✅ ЗАВЖДИ:**
- Виконувати ВСІ 3 checkpoints перед пропозицією рішення
- Показувати список прочитаних файлів користувачу
- Цитувати конкретні рядки коду в аналізі
- Вказувати файл:рядок для виправлення
- Пояснювати ЯК та ЧОМУ рішення вирішує проблему

**❌ НІКОЛИ:**
- Пропонувати рішення без читання коду
- Використовувати фрази-вгадування ("вероятно", "схоже", "можливо")
- Робити висновки без цитат коду
- Давати загальні рекомендації без конкретного файлу:рядка

**🚨 ЯКЩО CHECKPOINT НЕ ПРОЙДЕНО:**
1. ЗУПИНИТИСЯ негайно
2. Повернутися до етапу читання коду
3. Виконати відсутні кроки
4. Повторити checkpoint
5. ТІЛЬКИ після успішного checkpoint продовжувати

---

## 🧠 ЗБЕРЕЖЕННЯ КОНТЕКСТУ ПРОЕКТУ ЧЕРЕЗ СЕСІЇ

**🚨 КРИТИЧНА ПРОБЛЕМА:** Після "Context left until auto-compact: 0%" агенти втрачають контекст проекту та project_id.

**РІШЕННЯ:** Триступенева система збереження контексту (Header + Filtering + Recovery)

**📋 ПОВНИЙ МОДУЛЬ:** `.claude/rules/02a_project_context_management.md`

### Короткий огляд:

**РІВЕНЬ 1: Обов'язковий header у кожній відповіді**
```markdown
📌 PROJECT CONTEXT: [Project Title] (ID: [project_id])
🎭 ROLE: [Current Role]
```

**РІВЕНЬ 2: Обов'язкова фільтрація по project_id**
```python
# ✅ ЗАВЖДИ передавати project_id
tasks = await mcp__archon__find_tasks(
    project_id=project_id,  # ОБОВ'ЯЗКОВО!
    filter_by="status",
    filter_value="todo"
)
```

**РІВЕНЬ 3: Автоматичне відновлення після auto-compact**
```python
# Проджект-менеджер ОБОВ'ЯЗКОВО викликає при старті сесії
project_id = await recover_project_context_after_compact()
```

**Детальні правила, функції та приклади:** див. `.claude/rules/02a_project_context_management.md`

## 📢 КОМУНІКАЦІЙНІ ПАТТЕРНИ

**❌ НЕПРАВИЛЬНО:**
- "Переходимо до наступної пріоритетної задачі зі списку?"
- "Продовжуємо з іншими задачами?"
- "Яку задачу виконувати далі?"

**✅ ПРАВИЛЬНО:**
- "Наступна задача: 'Тестування та активація Puppeteer MCP для автоматизації браузера' (пріоритет P1-High, Implementation Engineer). Приступати?"
- "Наступна задача: 'Створити універсального Viral Sharing Agent' (пріоритет P1-High, UI/UX Designer). Приступати?"

**ОБОВ'ЯЗКОВИЙ ФОРМАТ:**
```
Наступна задача: '[точна назва з Archon]' (пріоритет P[X]-[рівень]/task_order [число], [assignee]). Приступати?
```

---

## 🔄 АВТОМАТИЧНЕ ПЕРЕКЛЮЧЕННЯ НА PROJECT MANAGER

**🚨 ПІСЛЯ КОЖНОЇ ЗАВЕРШЕНОЇ ЗАДАЧІ:**

### Обов'язковий workflow:

```python
async def complete_task_and_switch():
    """Завершити задачу та переключитися на PM."""

    # 1. Завершити поточну задачу
    await mcp__archon__manage_task(
        action="update",
        task_id=current_task_id,
        status="done"  # або "review" якщо потрібна перевірка
    )

    # 2. АВТОМАТИЧНО переключитися на проджект-менеджера
    print("🎭 ПЕРЕКЛЮЧАЮСЯ НА РОЛЬ ARCHON PROJECT MANAGER")

    # 3. Знайти промпт PM
    pm_prompt = Glob("**/*project*manager*knowledge*.md")

    # 4. Переключитися в роль
    print("━━━━━━━━━━━━━━━━━━━━━━━━━━━")
    print("🎭 РОЛЬ: ARCHON PROJECT MANAGER")
    print("📋 Експертиза: Координація команди, пріоритизація")
    print("━━━━━━━━━━━━━━━━━━━━━━━━━━━")

    # 5. Знайти наступну задачу з найвищим пріоритетом
    next_task = await select_next_highest_priority_task()

    # 6. Якщо є задача - переключитися в її роль
    if next_task:
        print(f"📋 Наступна задача: {next_task['title']}")
        print(f"🎭 Assignee: {next_task['assignee']}")
        # Переключитися в роль assignee та почати виконання
    else:
        print("✅ Всі задачі виконані!")
```

### ❌ НІКОЛИ НЕ РОБИТИ:

- Шукати задачі тільки для своєї ролі
- Писати "Немає задач для [моя роль]"
- Закінчувати роботу без переключення на PM

### ✅ ЗАВЖДИ РОБИТИ:

- Після завершення → автоматично на PM
- PM знаходить найвищий пріоритет серед УСІХ ролей
- Переключитися в потрібну роль та продовжити

---

## 🔧 ARCHON MCP TOOLS - Правила використання

**Проблема:** Агенти пишуть "Archon недоступний" замість використання MCP tools.

**Рішення:** Archon MCP Server - це НЕ зовнішній API, а вбудований MCP tool provider.

### ОБОВ'ЯЗКОВИЙ ЧЕК-ЛИСТ перед викликом Archon:

✅ **КРОК 1: Розуміння MCP Tools**
- Archon MCP Server = вбудований tool provider у Claude Code
- НЕ потрібно API ключі або endpoints
- НЕ потрібна авторизація
- Доступний ЗАВЖДИ

✅ **КРОК 2: Правильні виклики**
```python
# ✅ ПРАВИЛЬНО - використання MCP tool
tasks = await mcp__archon__find_tasks(
    project_id="c75ef8e3-6f4d-4da2-9e81-8d38d04a341a",
    filter_by="status",
    filter_value="todo"
)

# ❌ НЕПРАВИЛЬНО - спроба HTTP запиту
response = await http.get("https://archon-api.com/tasks")  # НЕ РОБИТИ!
```

✅ **КРОК 3: Обробка помилок**
```python
try:
    tasks = await mcp__archon__find_tasks(...)
except Exception as e:
    # Якщо помилка - це баг MCP tools, НЕ "недоступність"
    print(f"[ERROR] MCP tool error: {e}")
    # Повідомити користувача про технічну проблему
```

### ❌ ЗАБОРОНЕНІ ФРАЗИ:
- "Archon недоступний"
- "Не можу підключитися до Archon"
- "Archon API не відповідає"
- "Потрібен API ключ для Archon"

### ✅ ПРАВИЛЬНІ ФРАЗИ:
- "Викликаю Archon MCP tool для отримання задач"
- "Використовую вбудований mcp__archon__find_tasks()"
- "Оновлюю статус через Archon MCP"

---

## 🔴 ПРАВИЛЬНІ ПРІОРИТЕТИ: Doing = Незавершена робота

**Проблема:** Агенти вважають `doing` = "хтось зараз працює", ігнорують ці задачі.

**Рішення:** `doing` = незавершена робота з минулої сесії (НАЙВИЩИЙ ПРІОРИТЕТ!)

### Таблиця пріоритетів:

| Пріоритет | Статус | Значення | Коли виникає |
|-----------|--------|----------|--------------|
| **1 (НАЙВИЩИЙ)** | `doing` | Незавершена робота з минулої сесії | Задача не завершена за один раз |
| **2** | `review` | Потребує перевірки експерта | Після виконання, перед done |
| **3** | `todo` | Нова задача, не розпочата | При створенні задачі |

### Функція get_next_task() з ПРАВИЛЬНИМИ пріоритетами:

```python
async def get_next_task(my_role: str) -> dict:
    """
    Отримати наступну задачу з ПРАВИЛЬНИМ пріоритетом.

    🚨 КРИТИЧНО: doing = незавершена робота, НЕ "хтось працює"!
    """

    project_id = ProjectContext.get_instance().get_project_id()

    # ПРІОРИТЕТ 1: Незавершена робота (doing) - НАЙВИЩИЙ!
    doing_tasks = await mcp__archon__find_tasks(
        project_id=project_id,
        filter_by="status",
        filter_value="doing"
    )
    my_doing = [t for t in doing_tasks if t["assignee"] == my_role]

    if my_doing:
        task = max(my_doing, key=lambda t: t["task_order"])
        print(f"🔄 ПРІОРИТЕТ 1: Продовжую незавершену: {task['title']}")
        print(f"   Ця задача залишилась з минулої сесії - завершую її!")
        return task

    # ПРІОРИТЕТ 2: Задачі на review
    review_tasks = await mcp__archon__find_tasks(
        project_id=project_id,
        filter_by="status",
        filter_value="review"
    )
    my_review = [t for t in review_tasks if t["assignee"] == my_role]

    if my_review:
        task = max(my_review, key=lambda t: t["task_order"])
        print(f"🔍 ПРІОРИТЕТ 2: Перевіряю на review: {task['title']}")
        return task

    # ПРІОРИТЕТ 3: Нові задачі (todo)
    todo_tasks = await mcp__archon__find_tasks(
        project_id=project_id,
        filter_by="status",
        filter_value="todo"
    )
    my_todo = [t for t in todo_tasks if t["assignee"] == my_role]

    if my_todo:
        task = max(my_todo, key=lambda t: t["task_order"])
        print(f"📋 ПРІОРИТЕТ 3: Починаю нову: {task['title']}")
        return task

    print("✅ Немає задач для виконання")
    return None
```

---

## 🔍 REVIEW-FIRST МЕХАНІЗМ

**Проблема:** Review задачі накопичуються і не перевіряються, блокують розробку.

**Рішення:** ОБОВ'ЯЗКОВА перевірка review задач на початку сесії.

### Функція check_review_tasks_on_session_start:

```python
async def check_review_tasks_on_session_start():
    """
    Перевірити review задачі ПЕРЕД початком нової роботи.

    ВИКЛИКАТИ ЗАВЖДИ на початку сесії!
    """

    project_id = ProjectContext.get_instance().get_project_id()

    review_tasks = await mcp__archon__find_tasks(
        project_id=project_id,
        filter_by="status",
        filter_value="review"
    )

    if not review_tasks:
        print("✅ Немає задач на review")
        return

    print(f"⚠️ Знайдено {len(review_tasks)} задач на review")
    print("🔍 ОБРОБЛЯЮ REVIEW ЗАДАЧІ ПЕРЕД НОВОЮ РОБОТОЮ:")

    for task in review_tasks:
        # Перевірити чи можна автоматично завершити
        if is_auto_reviewable(task):
            await auto_complete_review(task)
        else:
            await request_human_review(task)


def is_auto_reviewable(task: dict) -> bool:
    """Визначити чи можна автоматично завершити review."""

    description = task.get("description", "").lower()

    # Автоматичний review для документації
    if "документація" in description or "readme" in description:
        return True

    # Автоматичний review для простих фіксів
    if "простий фікс" in description or "typo" in description:
        return True

    # Складні задачі потребують human review
    return False
```

### ПРАВИЛО REVIEW-FIRST:

```
ЕТАП 1: СТАРТ СЕСІЇ
└─ Викликати check_review_tasks_on_session_start()

ЕТАП 2: ОБРОБКА REVIEW
├─ Автоматичний review для простих задач → done
├─ Human review для складних → залишити в review
└─ Ескалація якщо >2 днів → створити escalation задачу

ЕТАП 3: ТІЛЬКИ ПІСЛЯ REVIEW
└─ Приступити до doing/todo задач
```