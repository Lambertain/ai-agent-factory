# -*- coding: utf-8 -*-
"""
ШАБЛОН: Инструменты для Pydantic AI агента

Этот шаблон содержит примеры инструментов для агента.
Инструменты помогают Claude в роли эксперта выполнять специализированные задачи.

ИСПОЛЬЗОВАНИЕ:
1. Замените [AGENT_NAME] на имя вашего агента (например, payment_integration)
2. Добавьте специализированные инструменты для вашей области экспертизы
3. Сохраните как agents/[agent_name]/tools.py

ВАЖНО: Эти инструменты используются ВАМИ (Claude) когда вы принимаете роль этого эксперта.
Это НЕ автономные агенты, а помощники для работы в роли.
"""

import logging
from typing import Optional, Dict, Any, List
from pydantic_ai import RunContext

logger = logging.getLogger(__name__)

# ============================================================================
# ИНСТРУМЕНТ 1: ПОИСК В БАЗЕ ЗНАНИЙ РОЛИ
# Помогает найти специализированную информацию когда вы в роли эксперта
# ============================================================================

async def search_agent_knowledge(
    ctx: RunContext,
    query: str,
    match_count: int = 5
) -> str:
    """
    Поиск в специализированной базе знаний роли через Archon RAG.

    Когда вы работаете в роли эксперта, этот инструмент помогает найти
    релевантную информацию из базы знаний этой роли.

    Args:
        ctx: Контекст выполнения с зависимостями
        query: Поисковый запрос
        match_count: Количество результатов

    Returns:
        Найденная информация из базы знаний роли
    """
    try:
        # Получаем теги знаний из зависимостей
        knowledge_tags = getattr(ctx.deps, 'knowledge_tags', [])
        knowledge_domain = getattr(ctx.deps, 'knowledge_domain', None)

        # Формируем запрос с учетом тегов роли
        search_query = f"{query} {' '.join(knowledge_tags)}"

        logger.info(f"Ищу в базе знаний роли: {search_query}")

        # ПРИМЕЧАНИЕ: Здесь должен быть вызов Archon RAG
        # Для шаблона оставляем заглушку
        # В реальном агенте: await mcp__archon__rag_search_knowledge_base(...)

        result = f"""
📚 **База знаний роли [{ctx.deps.agent_name}]:**

🔍 Запрос: {query}
📋 Теги: {', '.join(knowledge_tags)}
🌐 Домен: {knowledge_domain or 'Не указан'}

💡 **Найденная информация:**
[Здесь будут результаты поиска из Archon RAG]

ШАБЛОН: Замените на реальный вызов mcp__archon__rag_search_knowledge_base
"""

        return result

    except Exception as e:
        logger.error(f"Ошибка поиска в базе знаний: {e}")
        return f"❌ Ошибка поиска: {e}"


# ============================================================================
# ИНСТРУМЕНТ 2: АНАЛИЗ ЗАДАЧИ С ТОЧКИ ЗРЕНИЯ РОЛИ
# Помогает проанализировать задачу используя экспертизу роли
# ============================================================================

async def analyze_task_as_expert(
    ctx: RunContext,
    task_description: str,
    focus_area: Optional[str] = None
) -> str:
    """
    Анализировать задачу с точки зрения экспертизы роли.

    Когда вы работаете в роли эксперта, этот инструмент помогает
    структурировать анализ задачи согласно вашей специализации.

    Args:
        ctx: Контекст выполнения
        task_description: Описание задачи
        focus_area: Опциональная область фокуса

    Returns:
        Структурированный анализ задачи
    """
    try:
        logger.info(f"Анализирую задачу как эксперт {ctx.deps.agent_name}")

        # ШАБЛОН: Замените на реальную логику анализа специфичную для роли
        # Примеры для разных ролей:
        # - Security Expert: анализ уязвимостей, векторов атак
        # - Performance Expert: анализ bottleneck'ов, метрик
        # - UI/UX Expert: анализ usability, accessibility

        analysis = f"""
🎯 **Анализ задачи ({ctx.deps.agent_name}):**

**Задача:** {task_description}
**Область фокуса:** {focus_area or 'Общий анализ'}

**С точки зрения экспертизы роли:**
1. [Аспект 1 специфичный для роли]
2. [Аспект 2 специфичный для роли]
3. [Аспект 3 специфичный для роли]

**Рекомендации:**
- [Рекомендация 1]
- [Рекомендация 2]

**Риски и ограничения:**
- [Риск 1]
- [Риск 2]

ШАБЛОН: Замените на реальный анализ специфичный для вашей роли
"""

        return analysis

    except Exception as e:
        logger.error(f"Ошибка анализа задачи: {e}")
        return f"❌ Ошибка анализа: {e}"


# ============================================================================
# ИНСТРУМЕНТ 3: ПРИМЕНЕНИЕ ЭКСПЕРТИЗЫ К РЕШЕНИЮ
# Помогает применить специализированные знания роли к задаче
# ============================================================================

async def apply_expert_solution(
    ctx: RunContext,
    problem: str,
    constraints: Optional[List[str]] = None
) -> str:
    """
    Применить экспертизу роли к решению проблемы.

    Когда вы работаете в роли эксперта, этот инструмент помогает
    сформулировать решение используя best practices вашей специализации.

    Args:
        ctx: Контекст выполнения
        problem: Описание проблемы
        constraints: Список ограничений

    Returns:
        Решение с применением экспертизы
    """
    try:
        logger.info(f"Применяю экспертизу {ctx.deps.agent_name} к решению")

        constraints_list = constraints or []

        # ШАБЛОН: Замените на реальную логику решения специфичную для роли
        solution = f"""
💡 **Решение от эксперта ({ctx.deps.agent_name}):**

**Проблема:** {problem}
**Ограничения:** {', '.join(constraints_list) if constraints_list else 'Не указаны'}

**Предлагаемое решение:**
1. [Шаг 1 с применением экспертизы]
2. [Шаг 2 с применением экспертизы]
3. [Шаг 3 с применением экспертизы]

**Обоснование (почему это решение):**
- [Обоснование с точки зрения экспертизы]

**Best practices применённые:**
- [Best practice 1]
- [Best practice 2]

**Альтернативы:**
- [Альтернативный подход 1]
- [Альтернативный подход 2]

ШАБЛОН: Замените на реальное решение специфичное для вашей роли
"""

        return solution

    except Exception as e:
        logger.error(f"Ошибка применения решения: {e}")
        return f"❌ Ошибка решения: {e}"


# ============================================================================
# ИНСТРУМЕНТ 4: ЧЕКЛИСТ КАЧЕСТВА ДЛЯ РОЛИ
# Помогает проверить работу согласно стандартам роли
# ============================================================================

async def check_quality_standards(
    ctx: RunContext,
    work_description: str,
    work_type: str = "implementation"
) -> str:
    """
    Проверить работу согласно стандартам качества роли.

    Когда вы завершаете работу в роли эксперта, этот инструмент помогает
    проверить соответствие результата стандартам вашей специализации.

    Args:
        ctx: Контекст выполнения
        work_description: Описание выполненной работы
        work_type: Тип работы (implementation, analysis, review, documentation)

    Returns:
        Чеклист проверки качества
    """
    try:
        logger.info(f"Проверяю качество работы как {ctx.deps.agent_name}")

        # ШАБЛОН: Замените на реальные критерии качества специфичные для роли
        # Примеры для разных ролей:
        # - Security Expert: OWASP Top 10, CWE проверки
        # - Performance Expert: Core Web Vitals, метрики производительности
        # - UI/UX Expert: WCAG, usability heuristics

        checklist = f"""
✅ **Чеклист качества ({ctx.deps.agent_name}):**

**Тип работы:** {work_type}
**Описание:** {work_description[:200]}...

**Стандарты роли:**
□ [Критерий 1 специфичный для роли]
□ [Критерий 2 специфичный для роли]
□ [Критерий 3 специфичный для роли]

**Общие критерии:**
□ Универсальность (0% проект-специфичного кода)
□ Документация и примеры
□ Соответствие best practices

**Рекомендации для улучшения:**
- [Рекомендация 1]
- [Рекомендация 2]

ШАБЛОН: Замените на реальный чеклист специфичный для вашей роли
"""

        return checklist

    except Exception as e:
        logger.error(f"Ошибка проверки качества: {e}")
        return f"❌ Ошибка проверки: {e}"


# ============================================================================
# ДОПОЛНИТЕЛЬНЫЕ ИНСТРУМЕНТЫ ДЛЯ СПЕЦИАЛИЗАЦИИ
# Добавьте здесь инструменты специфичные для вашей роли
# ============================================================================

# ПРИМЕР: Инструмент для специфичной задачи роли
async def [agent_name]_specific_tool(
    ctx: RunContext,
    param1: str,
    param2: Optional[int] = None
) -> str:
    """
    Специфичный инструмент для задач роли.

    ШАБЛОН: Замените на реальный инструмент вашей специализации.
    Примеры:
    - Security: scan_for_vulnerabilities()
    - Performance: profile_performance()
    - UI/UX: check_accessibility()

    Args:
        ctx: Контекст выполнения
        param1: Параметр 1
        param2: Параметр 2 (опционально)

    Returns:
        Результат выполнения специфичного инструмента
    """
    try:
        logger.info(f"Выполняю специфичную задачу роли {ctx.deps.agent_name}")

        # ШАБЛОН: Реализуйте логику специфичную для вашей роли
        result = f"""
🔧 **Результат специфичного инструмента:**

Параметры: {param1}, {param2}

[Результаты выполнения специфичной задачи роли]

ШАБЛОН: Замените на реальную реализацию
"""

        return result

    except Exception as e:
        logger.error(f"Ошибка специфичного инструмента: {e}")
        return f"❌ Ошибка: {e}"


# ============================================================================
# ЭКСПОРТ ИНСТРУМЕНТОВ
# ============================================================================

__all__ = [
    "search_agent_knowledge",
    "analyze_task_as_expert",
    "apply_expert_solution",
    "check_quality_standards",
    "[agent_name]_specific_tool"
]
